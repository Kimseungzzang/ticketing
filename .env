DB_HOST=localhost
DB_PORT=5432
DB_NAME=ticket_read_db
DB_USERNAME=hbrc
DB_PASSWORD=
PORT=8083
KAFKA_HOST=localhost
KAFKA_PORT=9092
# 읽기 standby(스트리밍 복제). 미설정 시 primary(5432)로 폴백.
# standby를 5433에 띄웠다면(§16) 주석 해제 → 읽기 API가 standby에서 읽음.
# 기본 주석 처리: standby는 brew services가 자동기동 안 하므로, 하드 의존 시 재부팅 후 부팅 실패 위험.
#REPLICA_DB_HOST=localhost
#REPLICA_DB_PORT=5433
